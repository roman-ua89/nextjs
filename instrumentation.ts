/*
* a special file that allows you to execute code before the Next.js server starts and before any requests are handled.
* It's designed for tasks like initializing environment variables, connecting to databases,
* or performing other setup operations that need to occur only once when your application starts.
*
* */